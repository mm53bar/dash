#!/usr/bin/env ruby

begin
  require 'dash'
rescue LoadError
  require 'rubygems'
  require 'dash'
end

Dash.hash(ARGV) rescue puts "Help me out!  Gimme a domain and a password, kinda like `$ dash domain password'"
