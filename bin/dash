#!/usr/bin/env ruby

begin
  require 'dash'
rescue LoadError
  require 'rubygems'
  require 'dash'
end

Dash.hash(ARGV) rescue puts "Help me out!  Gimme a password and a domain, kinda like `$ dash password domain'"
